- name: Install & Run
  run: |
    npx clawhub@latest install verified-agent-identity --force
    node scripts/createNewEthereumIdentity.js
    node scripts/manualLinkHumanToAgent.js --challenge '{"name": "My_Dev_Agent", "description": "Telegram Mini App Developer Agent"}'
